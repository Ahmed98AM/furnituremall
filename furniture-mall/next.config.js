/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['static2.cilory.com', 'i-exc.ccm2.net']
  },
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig
